import 'dart:math';
import 'package:flutter/material.dart';

class Data {
  static Map<Color, double> colorsMap = {
    Colors.amber: 2 * pi,
    Colors.green: (1 / 4) * pi,
    Colors.pink: (1 / 2) * pi,
    Colors.grey: (3 / 4) * pi,
    Colors.brown: pi,
    Colors.orange: (5 / 4) * pi,
    Colors.red: (3 / 2) * pi,
    Colors.white: (7 / 4) * pi,
  };
}
